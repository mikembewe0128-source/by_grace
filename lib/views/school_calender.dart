import 'package:flutter/material.dart';

class SchoolCalender extends StatelessWidget {
  const SchoolCalender({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
